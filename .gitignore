# Maven
target/

# Environment variables
# FIX SEC-01: Both .env AND .env.example must be gitignored.
# .env.example previously contained REAL credentials (Razorpay, Google OAuth,
# Cloudinary, Gmail app passwords) committed to the repository.
# .env.example should only ever contain placeholder documentation — see the
# committed .env.example.template file for the safe template version.
.env
.env.example

# Logs
*.log

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbbs.db

# Node (if frontend exists later)
node_modules/
